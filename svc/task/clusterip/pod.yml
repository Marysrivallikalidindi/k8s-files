apiVersion: v1
kind: Pod
metadata:
  name: svc-pod
  labels:
    app: test
spec:
  containers:
    - name: testco-jenkins
      image: jenkins/jenkins:lts
      ports:
        - containerPort: 8080
          protocol: TCP